import 'package:flutter/material.dart';

class DiagnosisTab extends StatelessWidget {
  const DiagnosisTab({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Diagnosis'));
  }
}
